import ActivityDialogComponent from "./activity-dialog.vue";
const ActivityDialog = {
  install: function (Vue) {
    Vue.component("ActivityDialog", ActivityDialogComponent);
  }
};
export default ActivityDialog;
